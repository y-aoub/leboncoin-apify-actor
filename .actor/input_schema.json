{
  "title": "Leboncoin Scraper",
  "description": "Scrape Leboncoin listings from search URLs",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "urls_list": {
      "title": "Search URLs *",
      "type": "array",
      "description": "Enter one or more Leboncoin search URLs (must contain /recherche in the path). Category URLs (e.g. /cl/...) are not supported. This field is required.",
      "editor": "stringList",
      "items": {
        "type": "string"
      },
      "prefill": [
        "https://www.leboncoin.fr/recherche?category=9&locations=Paris&price=250000-400000&rooms=2-4&real_estate_type=2"
      ],
      "sectionCaption": "🎯 Search URLs",
      "sectionDescription": "Scrape one or multiple searches in a single run"
    },
    "max_pages": {
      "title": "Max pages",
      "type": "integer",
      "description": "Maximum number of pages to scrape. Set 0 to scrape all available pages (practical limit of 99999 pages). Recommended: 10-20 to start.",
      "editor": "number",
      "default": 10,
      "prefill": 10,
      "minimum": 0,
      "sectionCaption": "⚙️ Scraping settings",
      "sectionDescription": "Control volume and scraping speed"
    },
    "limit_per_page": {
      "title": "Ads per page",
      "type": "integer",
      "description": "Number of ads to scrape per page (Leboncoin typically displays 35 ads/page)",
      "editor": "number",
      "default": 35,
      "prefill": 35,
      "minimum": 1,
      "maximum": 100
    },
    "delay_between_pages": {
      "title": "Delay between pages (seconds)",
      "type": "number",
      "description": "Wait time between each page (0 = maximum speed recommended)",
      "editor": "number",
      "default": 0,
      "prefill": 0,
      "minimum": 0,
      "maximum": 5
    },
    "max_age_days": {
      "title": "Max ad age (days)",
      "type": "integer",
      "description": "Ignore ads older than X days (0 = disabled)",
      "editor": "number",
      "default": 0,
      "prefill": 0,
      "minimum": 0,
      "maximum": 365
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Use proxies to avoid blocking (recommended for high volume)",
      "editor": "proxy",
      "sectionCaption": "🔒 Anti-blocking protection",
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"],
        "apifyProxyCountry": "FR"
      }
    }
  },
  "required": ["urls_list"]
}
