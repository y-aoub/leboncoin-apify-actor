{
  "title": "Leboncoin Scraper - Mode URL",
  "description": "Scraper Leboncoin à partir d'une URL de recherche",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "direct_url": {
      "title": "URL de recherche Leboncoin",
      "type": "string",
      "description": "Collez l'URL complète de votre recherche Leboncoin (doit contenir /recherche dans le chemin). Les URLs de catégories (ex: /cl/...) ne sont pas supportées.",
      "editor": "textfield",
      "prefill": "https://www.leboncoin.fr/recherche?category=2&text=iphone&locations=Paris_75000",
      "sectionCaption": "🎯 URL de recherche",
      "sectionDescription": "Collez l'URL complète d'une page de recherche Leboncoin"
    },
    "max_pages": {
      "title": "Pages maximum",
      "type": "integer",
      "description": "Nombre maximum de pages à scraper. Recommandé: 10-20",
      "editor": "number",
      "default": 10,
      "minimum": 1,
      "maximum": 100,
      "sectionCaption": "⚙️ Paramètres de scraping",
      "sectionDescription": "Contrôlez le volume et la vitesse de scraping"
    },
    "limit_per_page": {
      "title": "Annonces par page",
      "type": "integer",
      "description": "Nombre d'annonces à scraper par page (Leboncoin affiche généralement 35 annonces/page)",
      "editor": "number",
      "default": 35,
      "minimum": 1,
      "maximum": 100
    },
    "delay_between_pages": {
      "title": "Délai entre pages (secondes)",
      "type": "number",
      "description": "Temps d'attente entre chaque page (0 = vitesse maximale recommandée)",
      "editor": "number",
      "default": 0,
      "minimum": 0,
      "maximum": 5
    },
    "max_age_days": {
      "title": "Âge maximum des annonces (jours)",
      "type": "integer",
      "description": "Ignorer les annonces plus vieilles que X jours (0 = désactivé)",
      "editor": "number",
      "default": 0,
      "minimum": 0,
      "maximum": 365
    },
    "proxyConfiguration": {
      "title": "Configuration proxy",
      "type": "object",
      "description": "Utiliser des proxies pour éviter les blocages (recommandé pour gros volumes)",
      "editor": "proxy",
      "sectionCaption": "🔒 Protection anti-blocage",
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"],
        "apifyProxyCountry": "FR"
      }
    },
    "output_format": {
      "title": "Format de sortie",
      "type": "string",
      "description": "Niveau de détail des données extraites",
      "editor": "select",
      "default": "detailed",
      "sectionCaption": "📤 Export des données",
      "enum": ["detailed", "compact"],
      "enumTitles": ["Détaillé (tous les champs)", "Compact (essentiel uniquement)"]
    }
  },
  "required": ["direct_url"]
}
