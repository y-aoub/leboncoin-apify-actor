{
  "title": "Leboncoin Scraper - Mode URL",
  "description": "Scraper Leboncoin à partir d'une URL de recherche",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "urls_list": {
      "title": "URLs à scraper *",
      "type": "array",
      "description": "Entrez une ou plusieurs URLs de recherche Leboncoin (doivent contenir /recherche dans le chemin). Les URLs de catégories (ex: /cl/...) ne sont pas supportées. Ce champ est obligatoire.",
      "editor": "stringList",
      "items": {
        "type": "string"
      },
      "sectionCaption": "🎯 URLs de recherche",
      "sectionDescription": "Scrapez une ou plusieurs recherches en une seule exécution"
    },
    "max_pages": {
      "title": "Pages maximum",
      "type": "integer",
      "description": "Nombre maximum de pages à scraper. Mettez 0 pour scraper toutes les pages disponibles (limite pratique de 99999 pages). Recommandé: 10-20 pour commencer.",
      "editor": "number",
      "default": 10,
      "minimum": 0,
      "sectionCaption": "⚙️ Paramètres de scraping",
      "sectionDescription": "Contrôlez le volume et la vitesse de scraping"
    },
    "limit_per_page": {
      "title": "Annonces par page",
      "type": "integer",
      "description": "Nombre d'annonces à scraper par page (Leboncoin affiche généralement 35 annonces/page)",
      "editor": "number",
      "default": 35,
      "minimum": 1,
      "maximum": 100
    },
    "delay_between_pages": {
      "title": "Délai entre pages (secondes)",
      "type": "number",
      "description": "Temps d'attente entre chaque page (0 = vitesse maximale recommandée)",
      "editor": "number",
      "default": 0,
      "minimum": 0,
      "maximum": 5
    },
    "max_age_days": {
      "title": "Âge maximum des annonces (jours)",
      "type": "integer",
      "description": "Ignorer les annonces plus vieilles que X jours (0 = désactivé)",
      "editor": "number",
      "default": 0,
      "minimum": 0,
      "maximum": 365
    },
    "proxyConfiguration": {
      "title": "Configuration proxy",
      "type": "object",
      "description": "Utiliser des proxies pour éviter les blocages (recommandé pour gros volumes)",
      "editor": "proxy",
      "sectionCaption": "🔒 Protection anti-blocage",
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"],
        "apifyProxyCountry": "FR"
      }
    }
  },
  "required": ["urls_list"]
}
