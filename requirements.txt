# Apify SDK
apify>=2.0.0

# HTTP engine used by the lbc client
curl-cffi==0.11.3

# Geocoding fallback requests (LeboncoinURLParser city lookup)
requests>=2.31.0

# lbc api - Public repository
git+https://github.com/y-aoub/lbc_.git

